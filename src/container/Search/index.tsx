import Input from "@components/InputComponents/Input";

const Search = () => {
  return <Input fullWidth variant={"outlined"} placeholder="Search Task" />;
};

export default Search;

// Pendding...........
// Apply throttling
// Display results (need to design and think)
// clean the code
// apply state
